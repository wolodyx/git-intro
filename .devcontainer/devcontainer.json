{
  "name": "github-intro",

  "build": {
    "dockerfile": "./Dockerfile"
  },
  "remoteUser": "bob",
  "postStartCommand": "${containerWorkspaceFolder}/continuous-build.sh",

  "runArgs": ["--network=host"],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "yzhang.markdown-all-in-one"
      ]
    }
  }
}
